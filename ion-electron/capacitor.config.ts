import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-electron',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
